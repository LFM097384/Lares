# Node
node_modules/
npm-debug.log

# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
app/pubspec.lock
*.iml
.idea/

# 环境与密钥(绝不提交 LiveKit 凭据)
.env
*.local

# OS
Thumbs.db
.DS_Store

# 运行时数据与日志
*.log
server/data/
server/tool/node_modules/

# LiveKit 开发用二进制(部署走官方镜像/下载,不入库)
server/livekit/

# Flutter 插件链接(Junction,指向本机 pub 缓存)
**/.plugin_symlinks/

# 本地下载的构建产物
dist/

# 临时缓存
.tmp-npm-cache/

# 部署运行时状态(VPS 上的端口基线/回滚快照,机器相关,不入库)
deploy/.state/
