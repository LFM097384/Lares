# 复制为 .env 并填写

# 域名(DNS A 记录指向 VPS;Caddy 自动签 TLS)
LARES_DOMAIN=rtc.example.com

# 下发给客户端的 LiveKit 公网地址(浏览器/移动端必须 wss)
LIVEKIT_PUBLIC_URL=wss://rtc.example.com

# LiveKit 凭据(生成:openssl rand -hex 16 / openssl rand -hex 32)
LIVEKIT_API_KEY=change-me-key
LIVEKIT_API_SECRET=change-me-secret
