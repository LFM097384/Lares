# 「一键进圈」语音陪伴 App 设计文档

> 主打:主屏幕点一下,即可进入一个小范围朋友圈子的公共语音空间,想来就来,想走就走。

---

## 一、产品思路评估

### 1.1 价值判断
这个思路的核心不是"语音聊天"(市面已经很多),而是**极低摩擦 + 极小圈子的"陪伴感"**:
- 低摩擦:一个 Tap(桌面小组件/快捷方式)直达,没有"发起邀请-等待接听"的社交成本。
- 小圈子:5~20 人的熟人圈,不是广场式陌生人语音房,不需要主播、排麦、公屏这些重机制。
- 常驻空间:不是"这次通话",而是"一个一直在那里的房间",类似"隔壁屋子有人在"。

这个方向是成立的,但**最大风险不是技术,是冷启动和留存**:
- 房间一旦长期空着,用户会失去"点一下"的习惯,产品死于"没人在线"。
- 语音是强"临场感"媒介,门槛比文字高,容易出现"尴尬的沉默"或"没人敢先开麦"。

### 1.2 已有的类似尝试(供参考,非结论性对标)
- Discord 的常驻语音频道 / "Hang"入房状态。
- 一些主打"一键跳入朋友视频/语音空间"的应用(如 Bunch 一类)。
- 国内的"云自习/云监督"类临时语音房。

这些产品普遍验证了需求存在,但活跃度普遍依赖**强绑定的既有关系链**(游戏搭子、班级、社团),而不是泛社交。建议你的产品**先绑定一个具体场景**(如"考研自习搭子""游戏开黑""异地情侣/好友日常陪伴"),而不是做成通用工具。

---

## 二、需求梳理

### 2.1 核心需求(MVP 必须有)
1. **一键进出**:主屏幕 Widget/快捷方式直达语音房,无需打开完整 App。
2. **小圈子(Circle)**:每个圈子是固定的一批好友,类似"群组",而非陌生人房间。
3. **在线状态感知**:能看到"谁在房间里""谁刚进来/刚离开",不看内容只看"人"。
4. **静音/耳返基础能力**:进房默认状态、一键静音、音量控制。
5. **低功耗、后台可维持连接的机制**(即使不追求 App 常驻后台,也要保证"点一下就能进"这个体验的响应速度)。
6. **隐私与防打扰**:谁能创建圈子、谁能邀请、房间是否需要"敲门"提示、免打扰时段。

### 2.2 建议补充的需求(容易被忽略但很关键)
| 需求 | 为什么重要 |
|---|---|
| **轻量状态表达**(如"在忙""想被叫醒""随时聊")| 解决"不知道现在方不方便进"的社交顾虑,降低进入门槛 |
| **进房提示的"温柔通知"**(不是电话式强提醒)| 语音房最怕打扰感太强,建议类似"轻敲门"而非来电铃声 |
| **多圈子隔离与切换**| 用户可能同时属于"家人群""死党群""搭子群",互不干扰 |
| **异步留言/语音便签**| 圈子里没人时,允许留一条 15 秒语音,回归"点一下"的轻量感 |
| **陪伴而非会议的 UI 语言**:去掉"发起会议""邀请与会者"这类office风格措辞 |
| **多端一致的在线presence同步**(手机/桌面/平板/车机等)| 用户可能在电脑前也想留在房间里 |
| **儿童/未成年人保护与举报机制**(若涉及熟人以外的扩展圈子)| 合规底线 |
| **耗电与流量透明度**(设置里显示"进房中"状态、可选仅WiFi高音质)| 常驻语音对续航敏感人群是硬需求 |

### 2.3 值得警惕、暂不做的需求
- 不建议一开始做"陌生人广场语音房"——会引入内容审核、主播运营等完全不同的复杂度。
- 不建议做"文字聊天/朋友圈信息流"的大而全社交产品——会稀释"一键进圈"的核心心智。

---

## 三、产品设计

### 3.1 核心用户旅程
```
桌面 Widget/快捷方式(显示圈子状态:X人在线)
        │ 一键点击
        ▼
   direct 进房(无需二次确认弹窗,除非圈子设置了"敲门"模式)
        │
        ▼
   房间内:头像/状态条(说话中高亮、静音图标、状态文案)
        │
        ├─ 想说话 → 取消静音说话
        ├─ 只想听 → 保持静音,挂机图标常显
        └─ 想走 → 一键退出,无需"告别"仪式感操作
```

### 3.2 关键界面状态
1. **桌面 Widget(核心入口)**
   - 展示:圈子名 + 当前在线人数/头像 + 一句轻量状态("阿伟在说话...")。
   - 空状态:显示"暂无人在,进去等等看?" 弱化"房间已死"的感觉。
2. **房间内界面**
   - 极简:头像网格 + 说话波纹动效 + 底部静音/离开两个主按钮。
   - 不做打字聊天区(保持语音优先),可给一个"表情反应"轻互动。
3. **多圈子管理**
   - 类似聊天 App 的会话列表,每个圈子一行,右侧显示在线人数徽标。

### 3.3 关键机制设计
- **敲门模式 vs 直接进入**:圈子创建者可设置"任何人可直接进"或"需轻量提示允许"。
- **闲时自动挂起**:房间无人说话超过 N 分钟,自动降级为"待机"状态以省电,UI 上仍显示"在线但安静"。
- **状态广播**:用"轻状态"代替"在线/离线"二元状态,比如"耳朵在""有事先走了"。

---

## 四、系统架构建议

### 4.1 总体架构图(逻辑视图)
```
┌─────────────┐   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐
│  iOS App    │   │ Android App │   │  Desktop App │   │   Web(轻量) │
│ + Widget    │   │ + Widget/   │   │ (Electron/   │   │  (可选)     │
│ (WidgetKit) │   │ 快捷方式     │   │  原生)       │   │             │
└──────┬──────┘   └──────┬──────┘   └──────┬──────┘   └──────┬──────┘
       │                 │                 │                 │
       └────────┬────────┴────────┬────────┴────────┬────────┘
                 │                 │                 │
          ┌──────▼─────────────────▼─────────────────▼──────┐
          │           接入网关 / API Gateway (HTTPS+WSS)      │
          └──────┬───────────────────────┬───────────────────┘
                 │                       │
     ┌───────────▼───────────┐   ┌───────▼────────────┐
     │  信令与状态服务 (Presence/│   │  业务服务(圈子/好友/  │
     │  Room State, WebSocket) │   │  权限/推送/审核)      │
     └───────────┬───────────┘   └───────┬────────────┘
                 │                       │
     ┌───────────▼───────────┐   ┌───────▼────────────┐
     │   音频媒体服务 (SFU)     │   │   数据存储层          │
     │  WebRTC / RTC SDK       │   │ (PostgreSQL/Redis/  │
     │  (自建或第三方RTC云)     │   │  对象存储)            │
     └─────────────────────────┘   └─────────────────────┘
```

### 4.2 分层建议

**客户端层(多端互通的关键)——v2 定案:Flutter-first**
- **定案:Flutter 单代码库覆盖 iOS / Android / Windows / macOS(后续可加 Web)**。理由:小团队没有资源维护 4 套原生 UI;Flutter 桌面端(Windows/macOS)已稳定,且 RTC 插件生态(LiveKit/Agora 均有官方 Flutter SDK)成熟;"界面要好看"在 Flutter 上靠自绘 UI 反而比拼凑各端原生风格更容易做出统一的高级感。
- **不做纯 Web 套壳**:语音延迟和后台保活对体验是生死线,Electron/Tauri 套壳方案在音频后台、功耗上都不达标,排除。
- **薄原生层只写三处**(平台差异无法被 Flutter 覆盖的部分):
  1. iOS WidgetKit 小组件 + App Intents(Widget 上直接进房);
  2. Android App Widget + Quick Settings Tile;
  3. 桌面托盘(macOS 菜单栏 / Windows 系统托盘,点击直接进房)。
  这些通过 `home_widget`、`tray_manager` 等成熟插件 + 少量原生代码实现,业务逻辑不下沉到原生层。
- KMP/Rust 核心层方案**降级为远期选项**:只有当团队扩张到需要分端精细打磨时,才把状态机/信令下沉为共享核心,现阶段收益抵不上成本。

**桌面入口(核心体验差异点)**
- iOS:WidgetKit + App Intents(iOS 17+ 可交互式 Widget,支持在 Widget 上直接触发进房 Intent,不强制跳转 App)。
- Android:App Widget + Quick Settings Tile 双入口,Widget 里可直接调用前台服务发起进房。
- 桌面(macOS/Windows):菜单栏/托盘常驻图标,点击直接进房,比 Widget 更容易做到"常驻";桌面端同时承担"挂机长在线"的角色(电脑前一挂一下午,正是本产品最典型的陪伴场景)。

**信令与状态服务**
- 职责:维护"谁在哪个房间""谁在说话""连接质量"等实时状态,基于 WebSocket 长连接广播。
- 建议用 Redis 做房间在线状态的高速读写 + Pub/Sub 广播,避免每次状态变化都打数据库。

**音频媒体服务(SFU)**
- 小圈子语音(≤20人)完全不需要自建复杂的 MCU/SFU 集群,**建议 MVP 阶段直接采购成熟 RTC 云服务**(声网 Agora、腾讯云 TRTC、LiveKit Cloud、Zego 等),把精力放在产品体验而不是音频链路优化上。
- 等用户规模和成本压力到一定阶段,再评估自建 SFU(如基于开源 LiveKit / mediasoup 自部署)。

**推送与唤醒**
- "轻敲门"提示走静默推送(iOS Silent Push / Android FCM 高优先级)而非强提醒式推送,避免打扰感。
- 需要处理"进房但 App 被系统杀后台"的场景:iOS 上用 PushKit + CallKit(哪怕不是电话场景,也可以借助 CallKit 的唤醒能力,但要注意 App Store 审核对 CallKit 用途的限制,需要评估是否合规,建议先用普通推送 + 快速冷启动优化)。

**数据与存储**
- 关系型库(圈子、成员、权限关系):PostgreSQL。
- 实时状态/在线信息:Redis。
- 若要支持语音留言/回放:对象存储(S3 兼容)+ 音频转码队列。

### 4.3 多端互通的关键难点
1. **状态一致性**:同一账号在手机和电脑同时开着 App 时,"在房间里"的状态如何唯一化——建议设计"多端在线但只有一端为音频主设备"的机制,类似 Spotify Connect 的"设备切换"体验。
2. **进房延迟**:一键触达的心智要求"点击到听到声音"控制在 1 秒左右,这对信令服务的响应速度和 RTC SDK 的连接建立速度要求很高,需要做"预连接"优化(比如打开 Widget 详情时提前建立信令连接)。
3. **后台保活与省电的平衡**:安卓端后台保活容易被系统限制(各厂商定制系统差异大),需要做好"仅在真正进房时才占用后台资源"的精细化管理,不能做成"常驻高耗电服务"。

---

## 五、技术选型建议(总结表)

| 模块 | 建议方案(MVP阶段) | 后续演进 |
|---|---|---|
| 跨端框架 | **Flutter**(iOS/Android/Windows/macOS 单代码库)+ 薄原生层(Widget/托盘) | 视团队规模评估核心逻辑下沉 KMP/Rust |
| 音视频通信 | **LiveKit(Cloud 起步)**;若主战场在国内且网络/合规要求强,备选声网 Agora / 腾讯 TRTC | 规模化后自托管开源 LiveKit SFU(平滑,无需换 SDK) |
| 信令/在线状态 | WebSocket + Redis Pub/Sub | 引入独立状态服务集群 |
| 后端服务 | Node.js/Go 均可,推荐 Go(并发与长连接场景友好) | 微服务拆分 |
| 数据库 | PostgreSQL + Redis | 视数据量分库分表 |
| 推送 | APNs + FCM | 结合业务优化触达策略 |
| 桌面入口 | WidgetKit(iOS)/ App Widget(Android)/ 托盘图标(Desktop) | 车机/手表等更多端入口 |

---

## 六、分阶段路线图建议

**第一阶段(验证核心体验,4~8周)**
- 单一圈子(不做多圈子管理)、直接进房(不做敲门机制)。
- 采用第三方 RTC 云 + 简单信令服务。
- 只做 iOS 或 Android 其中一端 + Widget,先验证"一键进圈"这个体验是否真的被高频使用。

**第二阶段(打磨留存,8~12周)**
- 多圈子支持、轻状态表达、敲门模式、多端(补齐另一端 + 桌面)。
- 引入"闲时挂起省电"和"预连接"优化。

**第三阶段(规模化)**
- 视活跃数据决定是否自建 SFU、是否拓展到"多个圈子发现/半熟人"场景。
- 完善举报/隐私/未成年人保护机制(如果用户规模扩大超出纯熟人圈)。

---

## 七、主要风险清单

1. **冷启动风险**:房间常年无人,建议第一版就强绑定一个具体使用场景做种子用户测试,而不是泛化推广。
2. **平台审核风险**:iOS 对 Widget 交互能力、后台唤醒机制有较严格限制,App Intents/CallKit 的使用需提前研究最新审核规则。
3. **隐私与安全**:语音数据加密传输(RTC 自带 DTLS/SRTP)、是否留存录音需明确告知用户并可关闭。
4. **成本风险**:第三方 RTC 云按分钟计费,常驻语音房若做成"挂着不说话也算在线"的模式,需评估云服务成本曲线,建议闲时自动降级/断开媒体流只保留信令在线状态。

---

## 八、多端互联修订方案(v2)

> 本节为"苹果 + Windows 等多端互联、界面精致、性能优化"目标下的定案,与第四~六节冲突处以本节为准。

### 8.1 RTC 选型:LiveKit 优先

| 候选 | 判断 |
|---|---|
| **LiveKit(首选)** | 官方 Flutter SDK 质量高(含桌面端);开源 SFU,Cloud 起步、规模化后可无缝转自托管,不被单一云厂商锁死;小房间(≤20人)场景下 simulcast/带宽自适应开箱即用 |
| 声网 Agora | 国内网络质量与弱网优化最强,Flutter SDK 成熟;缺点是闭源、按分钟计费成本高、无自托管退路 |
| 腾讯 TRTC | 国内合规与价格友好;跨端 SDK 质量与生态弱于前两者 |

**结论**:默认 LiveKit Cloud 起步;若种子用户全部在国内且实测连通性不达标,切 Agora(两者 API 层做薄抽象,切换成本可控)。

### 8.2 UI 精致的落地路径

Flutter 上做出高级感的纪律:
1. **先建设计系统再画页面**:token 化颜色/圆角/字阶/间距(单一 `AppTheme` 数据源),杜绝散落的硬编码数值。
2. **暗色模式优先**:陪伴型语音产品的典型场景是夜间挂机,暗色是一等公民,亮色做适配而非反过来。
3. **品牌感靠自绘**:说话波纹、房间"呼吸感"氛围背景、头像状态环,用 CustomPainter + 动画控制器实现,这是"好看"的记忆点,不要用素材贴图凑。
4. **平台适配的取舍**:不为 Material/Cupertino 分裂维护两套——做一套自有品牌风格,仅在导航习惯、滚动物理、窗口框架(桌面自绘标题栏 + 无边框窗口)上尊重平台惯例。
5. **桌面端独立布局断点**:桌面不是"放大的手机",房间内桌面用侧栏(圈子列表)+ 主区(房间)布局,移动端用底部导航。

### 8.3 性能/功耗优化清单(按优先级)

| 优先级 | 项 | 目标 |
|---|---|---|
| P0 | **预连接**:Widget/托盘展示在线状态时即建立信令长连接并预热 RTC | 点击到听到声音 ≤ 1.5s(MVP 目标),终态 ≤ 1s |
| P0 | **闲时媒体降级**:无人说话 N 分钟自动断开媒体流只留信令 presence | 挂机功耗/流量/云成本可接受 |
| P1 | **后台保活精细化**:仅进房期间持有前台服务/音频会话,出房立即释放 | 不被系统杀、不常驻耗电 |
| P1 | **多端 presence 一致性**:同一账号多端在线时"音频主设备"唯一化(Spotify Connect 式切换) | 状态不错乱 |
| P2 | 渲染优化:Impeller、列表/动画 const 化、波形动画降帧(挂机时 30fps→静态) | 全程 60/120fps 无掉帧 |
| P2 | 冷启动优化(移动端) | 冷启动到可进房 ≤ 2s |

### 8.4 修订后的第一阶段计划(4 周)

1. **第 1 周:骨架互通**。Flutter 工程 + 设计系统 token + LiveKit Cloud 沙箱,跑通"一个固定圈子房间"在 iOS/Android/Windows/macOS 四端同时进房、互相对讲。
2. **第 2 周:核心体验**。房间内 UI(头像网格 + 说话波纹 + 静音/离开)、presence 信令服务(Node/Go + Redis 可从简,先用内存版)、进房预连接。
3. **第 3 周:入口与桌面**。Windows 托盘 + macOS 菜单栏一键进房;移动端先用 App 内一键入口,Widget 原生层视进度插入。
4. **第 4 周:硬化**。闲时降级、后台保活、断线重连、暗色模式打磨、实机延迟与功耗测试,拉 3~5 个真实熟人开始日常挂用。

**验收标准**:四端同房间互通稳定;点击进房到听到声音 ≤1.5s;挂机 2 小时手机耗电可感知但不烫手;桌面端可作为"常驻挂机端"连续在线一天不掉线。

---

## 九、实现状态(2026-09 更新)

> 与 §8.4 四周计划的对照。仓库:`server/`(信令+token+便签 API)、`app/`(Flutter 四端+Web)。

### 已落地并验证(e2e/单测)

| 项 | 状态 | 证据 |
|---|---|---|
| Flutter 四端+Web 工程 | ✅ 三端构建通过(Windows exe/Android APK/Web),iOS/macOS 代码就绪 | VS2019+便携 CMake、Junction 绕过开发者模式 |
| 设计系统(暗色优先、token 化) | ✅ 余烬暖橙品牌、自绘说话波纹、呼吸背景、布局断点 | `app/lib/src/theme/`、截图 |
| presence 信令 + 大厅摘要 | ✅ 未进房可见「X 人在 · 名字」 | 服务端 16 项冒烟 |
| 一键进房 ≤1.5s | ✅ 桌面冷 1.38s/热 1.06s;模拟器 2.1s(虚网卡) | token 预取+并行,分段计时 |
| LiveKit RTC | ✅ 本地 dev server 全链路互通 | 双端实测对讲连接建立 |
| 断线重连/闲时媒体降级 | ✅ 重连自动回房;5 分钟无说话断媒体留 presence,来人唤醒 | 单测覆盖 |
| 桌面托盘/Widget/Tile/深链入口 | ✅ 五入口:App、Android Widget、QS Tile、桌面托盘、`lares://join` 深链 | 模拟器深链实测 |
| 改昵称、多圈子、敲门模式、语音便签 | ✅ 全部 e2e 验证;敲门含"未放行不连媒体"隐私门控 | 10 项客户端测试 |
| 圈子邀请 | ✅ `lares://circle` 深链 + 复制/粘贴 | 模拟器冷启直达实测 |
| 品牌图标 | ✅ 三端 launcher/tray/favicon 替换,应用名「一键进圈」 | `tool/make_*.ps1` |

### 与设计的偏差

1. **RTC 选了 LiveKit**(§8.1 已定),本地用官方单二进制 `--dev` 起步,零凭据;`--bind 0.0.0.0 --node-ip <LAN>` 才支持模拟器/真机。
2. **播放库用 media_kit** 而非 audioplayers(VS2019 编不过 cppwinrt)。
3. **圈子为本地登记 + 深链邀请**(无服务端注册表),符合熟人小圈定位;接账号后迁移。

### 待外部条件

- 实体 Android 机:延迟/功耗终验(模拟器数据偏保守)。
- Mac:iOS/macOS 构建 + WidgetKit 入口。
- LiveKit Cloud 凭据或公网自托管:跨网互通。
- 桌面挂机一天稳定性长测(需自然时间)。