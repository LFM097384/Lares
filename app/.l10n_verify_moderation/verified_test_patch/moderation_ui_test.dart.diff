diff --git "a/D:\\Projects\\Lares\\app\\test\\moderation_ui_test.dart" "b/D:\\Projects\\Lares\\.l10n_verify_moderation_app\\test\\moderation_ui_test.dart"
index b8767d6..39fcf30 100644
--- "a/D:\\Projects\\Lares\\app\\test\\moderation_ui_test.dart"
+++ "b/D:\\Projects\\Lares\\.l10n_verify_moderation_app\\test\\moderation_ui_test.dart"
@@ -3,6 +3,8 @@ import 'dart:typed_data';
 
 import 'package:flutter/material.dart';
 import 'package:flutter_test/flutter_test.dart';
+import 'package:lares_app/l10n/gen/app_localizations.dart';
+import 'package:lares_app/l10n/gen/app_localizations_zh.dart';
 import 'package:lares_app/src/chat/chat_envelope.dart';
 import 'package:lares_app/src/chat/chat_message.dart';
 import 'package:lares_app/src/chat/chat_service.dart';
@@ -19,6 +21,22 @@ import 'package:lares_app/src/ui/room_screen.dart';
 import 'package:lares_app/src/ui/widgets/avatar_orb.dart';
 import 'package:shared_preferences/shared_preferences.dart';
 
+/// 举报分类在测试里的显示名。
+///
+/// 界面文案已本地化,测试用的是 zh(测试 harness 固定 Locale('zh')),
+/// 所以这里直接查 zh 的生成类,而不是把中文字面量再抄一遍 ——
+/// 抄一遍就等于文案改了测试还绿。
+final AppLocalizations _zh = AppLocalizationsZh();
+
+String reasonText(ReportReason r) => switch (r) {
+      ReportReason.harassment => _zh.reportReasonHarassment,
+      ReportReason.hateSpeech => _zh.reportReasonHateSpeech,
+      ReportReason.sexualContent => _zh.reportReasonSexualContent,
+      ReportReason.violence => _zh.reportReasonViolence,
+      ReportReason.illegal => _zh.reportReasonIllegal,
+      ReportReason.spam => _zh.reportReasonSpam,
+      ReportReason.other => _zh.reportReasonOther,
+    };
 /// 假信令:不碰真实 socket,只记录发出的消息(与 room_screen_test.dart 同款)
 class FakeSignalingClient extends SignalingClient {
   FakeSignalingClient() : super(url: 'ws://fake');
@@ -136,7 +154,10 @@ class FakeTransport implements ChatTransport {
 
 /// 统一宿主:固定暗色主题 + Scaffold(SnackBar 需要 ScaffoldMessenger)
 Widget _host(Widget child) =>
-    MaterialApp(theme: LaresTheme.dark(), home: Scaffold(body: child));
+    MaterialApp(
+      locale: const Locale('zh'),
+      localizationsDelegates: AppLocalizations.localizationsDelegates,
+      supportedLocales: AppLocalizations.supportedLocales,theme: LaresTheme.dark(), home: Scaffold(body: child));
 
 /// 展开态的 ChatPanel 收到新消息会起一段滚动动画([_scrollDuration] 160ms),
 /// 用例结束时它可能还没跑完,框架就会报
@@ -214,6 +235,9 @@ void main() {
 
       await tester.pumpWidget(
         MaterialApp(
+      locale: const Locale('zh'),
+      localizationsDelegates: AppLocalizations.localizationsDelegates,
+      supportedLocales: AppLocalizations.supportedLocales,
           theme: LaresTheme.dark(),
           home: RoomScreen(
             controller: controller,
@@ -242,6 +266,9 @@ void main() {
 
       await tester.pumpWidget(
         MaterialApp(
+      locale: const Locale('zh'),
+      localizationsDelegates: AppLocalizations.localizationsDelegates,
+      supportedLocales: AppLocalizations.supportedLocales,
           theme: LaresTheme.dark(),
           home: RoomScreen(
             controller: controller,
@@ -266,6 +293,9 @@ void main() {
 
       await tester.pumpWidget(
         MaterialApp(
+      locale: const Locale('zh'),
+      localizationsDelegates: AppLocalizations.localizationsDelegates,
+      supportedLocales: AppLocalizations.supportedLocales,
           theme: LaresTheme.dark(),
           home: RoomScreen(
             controller: controller,
@@ -313,6 +343,9 @@ void main() {
 
       await tester.pumpWidget(
         MaterialApp(
+      locale: const Locale('zh'),
+      localizationsDelegates: AppLocalizations.localizationsDelegates,
+      supportedLocales: AppLocalizations.supportedLocales,
           theme: LaresTheme.dark(),
           home: RoomScreen(
             controller: controller,
@@ -346,6 +379,9 @@ void main() {
 
       await tester.pumpWidget(
         MaterialApp(
+      locale: const Locale('zh'),
+      localizationsDelegates: AppLocalizations.localizationsDelegates,
+      supportedLocales: AppLocalizations.supportedLocales,
           theme: LaresTheme.dark(),
           home: RoomScreen(controller: controller, circleName: '我们的圈'),
         ),
@@ -554,11 +590,11 @@ void main() {
 
       // 七个分类都摆出来了,但一个都没选中
       for (final ReportReason r in ReportReason.values) {
-        expect(find.text(r.label), findsOneWidget);
+        expect(find.text(reasonText(r)), findsOneWidget);
       }
       expect(submitHandler(tester), isNull);
 
-      await tester.tap(find.text(ReportReason.harassment.label));
+      await tester.tap(find.text(reasonText(ReportReason.harassment)));
       await tester.pumpAndSettle();
 
       expect(submitHandler(tester), isNotNull);
@@ -575,7 +611,7 @@ void main() {
         messageExcerpt: '一段很难听的话',
       );
 
-      await tester.tap(find.text(ReportReason.hateSpeech.label));
+      await tester.tap(find.text(reasonText(ReportReason.hateSpeech)));
       await tester.pumpAndSettle();
       await tester.enterText(find.byType(TextField), '他连着说了三遍');
       await tester.pumpAndSettle();
@@ -606,7 +642,7 @@ void main() {
       final BlockStore blocks = await BlockStore.load();
       await openReportFlow(tester, blocks: blocks);
 
-      await tester.tap(find.text(ReportReason.spam.label));
+      await tester.tap(find.text(reasonText(ReportReason.spam)));
       await tester.pumpAndSettle();
       await tester.tap(find.text('提交举报'));
       await tester.pumpAndSettle();
@@ -628,7 +664,7 @@ void main() {
       await blocks.block('u_other');
       await openReportFlow(tester, blocks: blocks);
 
-      await tester.tap(find.text(ReportReason.other.label));
+      await tester.tap(find.text(reasonText(ReportReason.other)));
       await tester.pumpAndSettle();
       await tester.tap(find.text('提交举报'));
       await tester.pumpAndSettle();
@@ -644,7 +680,7 @@ void main() {
       final List<ReportDraft> delivered =
           await openReportFlow(tester, blocks: blocks);
 
-      await tester.tap(find.text(ReportReason.violence.label));
+      await tester.tap(find.text(reasonText(ReportReason.violence)));
       await tester.pumpAndSettle();
       await tester.tap(find.text('算了'));
       await tester.pumpAndSettle();
